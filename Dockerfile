
# Start from the official n8n image
FROM docker.n8n.io/n8nio/n8n:latest

# Install Chromium and Puppeteer dependencies
RUN apt-get update && apt-get install -y \
    wget \
    ca-certificates \
    fonts-liberation \
    libappindicator3-1 \
    libasound2 \
    libatk-bridge2.0-0 \
    libatk1.0-0 \
    libcups2 \
    libdbus-1-3 \
    libgdk-pixbuf2.0-0 \
    libnspr4 \
    libnss3 \
    libx11-xcb1 \
    libxcomposite1 \
    libxdamage1 \
    libxrandr2 \
    xdg-utils \
    --no-install-recommends && \
    rm -rf /var/lib/apt/lists/*

# Install Puppeteer
RUN npm install puppeteer

# Set Puppeteer environment variable for Chromium path
ENV PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
